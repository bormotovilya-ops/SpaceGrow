# Настройка GigaChat на Vercel (БЕСПЛАТНО!)

## Краткий ответ на ваши вопросы:

### 1. Нужен ли backend для GigaChat?
**Да, нужен**, но можно использовать **Vercel Serverless Functions** - это бесплатно!

### 2. Получится опубликовать на Vercel бесплатно?
**Да, полностью бесплатно!** Vercel предоставляет:
- ✅ 100GB-hours бесплатно в месяц (более чем достаточно для чат-бота)
- ✅ Бесплатный домен `your-project.vercel.app`
- ✅ Автоматический деплой из GitHub
- ✅ SSL сертификат бесплатно

## Пошаговая инструкция:

### Шаг 1: Получите API ключ GigaChat

1. Зарегистрируйтесь на https://developers.sber.ru/gigachat
2. Создайте приложение
3. Получите API ключ
4. Сохраните его - понадобится на следующем шаге

### Шаг 2: Настройте переменные окружения в Vercel

1. Войдите в ваш проект на https://vercel.com
2. Перейдите в **Settings** → **Environment Variables**
3. Нажмите **Add New**
4. Заполните:
   - **Name:** `GIGACHAT_API_KEY`
   - **Value:** ваш API ключ от GigaChat
   - Выберите все окружения: ✅ Production, ✅ Preview, ✅ Development
5. Нажмите **Save**

### Шаг 3: Деплой проекта

Файл `api/chat.js` уже создан в проекте. Просто:

```bash
git add .
git commit -m "Add GigaChat integration"
git push
```

Vercel автоматически задеплоит проект и Serverless Function будет работать!

### Шаг 4: Проверка

После деплоя проверьте:
- Откройте ваш сайт на Vercel
- Откройте консоль браузера (F12)
- Попробуйте отправить запрос к `/api/chat`

## Как это работает:

1. **Frontend** (`src/components/ChatBot.jsx`) отправляет запрос на `/api/chat`
2. **Vercel** автоматически направляет запрос к Serverless Function `api/chat.js`
3. **Serverless Function** вызывает GigaChat API с вашим ключом
4. **Ответ** возвращается пользователю

## Преимущества Vercel Serverless Functions:

✅ **Бесплатно** - 100GB-hours в месяц
✅ **Безопасно** - API ключ хранится в переменных окружения, не в коде
✅ **Масштабируемо** - автоматически масштабируется под нагрузку
✅ **Быстро** - ответы за миллисекунды
✅ **Просто** - не нужен отдельный сервер

## Стоимость:

- **Vercel:** Бесплатно (100GB-hours/месяц)
- **GigaChat:** Бесплатно (до 10,000 токенов/день)
- **Итого:** Полностью бесплатно! 🎉

## Если нужно вернуть кнопку чата:

В файле `src/components/Header.jsx` раскомментируйте блок с кнопкой чата.

## Важно про GigaChat API:

GigaChat может требовать **OAuth авторизацию** вместо простого Bearer токена. 

Если получаете ошибки авторизации:
1. Проверьте документацию GigaChat: https://developers.sber.ru/gigachat/docs
2. Возможно, нужно сначала получить access token через OAuth
3. Или используйте другой формат авторизации

Альтернатива: можно использовать библиотеку `gigachat` для Python или Node.js, которая автоматически обрабатывает авторизацию.

## Устранение неполадок:

### Ошибка "GigaChat API не настроен"
- Проверьте, что переменная `GIGACHAT_API_KEY` добавлена в Vercel
- Убедитесь, что выбраны все окружения (Production, Preview, Development)
- Передеплойте проект после добавления переменной

### Ошибка авторизации GigaChat
- GigaChat может требовать OAuth токен вместо API ключа
- Проверьте актуальную документацию на developers.sber.ru
- Возможно, нужно использовать библиотеку для авторизации

### Ошибка 404 на `/api/chat`
- Убедитесь, что файл `api/chat.js` существует в папке `api/` в корне проекта
- Проверьте, что проект задеплоен на Vercel
- Vercel автоматически распознает папку `api/` как Serverless Functions

### Ошибка CORS
- Serverless Function уже настроена с правильными CORS заголовками
- Если проблемы остаются, проверьте настройки в `api/chat.js`
