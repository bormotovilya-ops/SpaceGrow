# Система автоматического логирования пользовательских действий

## Обзор

Реализована комплексная система логирования всех пользовательских действий на сайте и в MiniApp с автоматической сегментацией пользователей и аналитикой поведения.

## Архитектура системы

### Основные компоненты

1. **Расширенная база данных** - 8 таблиц с детальным логированием
2. **API эндпоинты** - 9 специализированных эндпоинтов для логирования
3. **Система сегментации** - автоматическое определение сегментов пользователей
4. **Аналитика** - инструменты для анализа поведения и конверсии

### Таблицы базы данных

#### `site_sessions` (расширенная)
- Источник трафика (UTM, referrer)
- Информация об устройстве (device, OS, browser)
- Геолокация (IP-based)
- Метрики сессии (duration, page_views, events_count)

#### `site_events` (расширенная)
- Категории и подтипы событий
- Детали взаимодействия (scroll_depth, time_spent)
- Контекст (section, element_id, previous_event_id)
- Кастомные данные в JSON

#### Новые специализированные таблицы:
- `ai_interactions` - взаимодействия с AI
- `content_views` - просмотр контента
- `cta_clicks` - клики по CTA
- `game_actions` - игровые действия

## Логируемые события

### 1. Источники посещений
```javascript
// Автоматическое логирование при первом заходе
{
  "session_id": 123,
  "source": "telegram|vk|direct|search|landing",
  "utm_params": {...},
  "referrer": "https://t.me/channel"
}
```

### 2. Открытие MiniApp
```javascript
// При запуске MiniApp
{
  "device": "iPhone 12",
  "page_id": "diagnostics",
  "timestamp": "2026-01-23T10:00:00"
}
```

### 3. Просмотр контента
```javascript
// При прокрутке и времени на странице
{
  "content_type": "section",
  "content_id": "about",
  "time_spent": 120,
  "scroll_depth": 85,
  "section": "main"
}
```

### 4. AI взаимодействие
```javascript
// После завершения разговора (кроме экспертных)
{
  "messages_count": 8,
  "topics": ["pricing", "diagnostics"],
  "duration": 240,
  "conversation_type": "general"
}
```

### 5. Диагностика и тесты
```javascript
// При завершении диагностики
{
  "results": {"score": 85, "category": "A"},
  "start_time": "2026-01-23T10:00:00",
  "end_time": "2026-01-23T10:15:00",
  "progress": {"completed": 100}
}
```

### 6. Игровые действия
```javascript
// При каждом значимом действии в игре
{
  "game_type": "calculator",
  "action_type": "achievement",
  "action_data": {"level": 5},
  "score": 2000,
  "achievement": "master_calculator"
}
```

### 7. CTA клики
```javascript
// При клике на любой призыв к действию
{
  "cta_type": "call",
  "cta_location": "header",
  "previous_step": "diagnostic_complete",
  "step_duration": 45
}
```

### 8. Персональный путь
```javascript
// При просмотре PDF или персональной страницы
{
  "open_time": "2026-01-23T10:00:00",
  "duration": 300,
  "downloaded": true
}
```

## Исключения из логирования

- **Экспертные разговоры** - разговоры с экспертами и закрытие сделок не логируются
- **Системные события** - технические ошибки, автоматические действия
- **Чувствительные данные** - пароли, платежная информация

## Система сегментации пользователей

### Сегменты

1. **newcomer** - новые пользователи (1 сессия, нет диагностики)
2. **engaged** - вовлеченные (3+ сессии, 15+ событий)
3. **converter** - прошедшие диагностику
4. **loyal** - высоко вовлеченные (10+ сессий, 50+ событий)

### Уровни вовлеченности

- **low** - < 3 сессий или < 15 событий
- **medium** - 3-9 сессий или 15-49 событий
- **high** - 10+ сессий или 50+ событий

### Факторы сегментации

- **Поведенческие паттерны:**
  - source_telegram, source_vk, source_direct
  - active_morning, active_evening, active_night
  - frequent_visitor, regular_visitor

- **Предпочтения контента:**
  - likes_section, likes_article, likes_video
  - ai_general, ai_diagnostic

## Автоматические действия

### Триггеры на основе сегментов

```python
# Для новичков
if segment == 'newcomer':
    trigger_welcome_message(user_id)
    show_onboarding_tour()

# Для вовлеченных
if segment == 'engaged':
    send_diagnostic_reminder(user_id)
    show_personal_offers()

# Для конвертеров
if segment == 'converter':
    recommend_related_content(user_id)
    show_upsell_opportunities()
```

## Интеграция в frontend

### JavaScript SDK для логирования

```javascript
// Инициализация
const tracker = new UserTracker({
  sessionId: currentSessionId,
  userId: tgUserId,
  cookieId: getCookie('user_session')
});

// Автоматическое логирование
tracker.trackSourceVisit({
  source: detectSource(),
  utmParams: parseUtmParams(),
  referrer: document.referrer
});

tracker.trackContentView({
  contentId: 'about_section',
  timeSpent: 120,
  scrollDepth: 85
});

tracker.trackCtaClick({
  ctaType: 'call',
  ctaLocation: 'header',
  previousStep: getLastStep()
});
```

### Автоматическое отслеживание

```javascript
// Отслеживание времени на странице
let pageStartTime = Date.now();

window.addEventListener('beforeunload', () => {
  const timeSpent = Math.floor((Date.now() - pageStartTime) / 1000);
  tracker.trackContentView({
    contentId: currentPageId,
    timeSpent: timeSpent,
    scrollDepth: getScrollDepth()
  });
});

// Отслеживание прокрутки
let maxScrollDepth = 0;
window.addEventListener('scroll', () => {
  const currentDepth = getScrollDepth();
  maxScrollDepth = Math.max(maxScrollDepth, currentDepth);
});
```

## Аналитические возможности

### Воронка конверсии

```javascript
const funnel = await api.getConversionFunnel();
console.log(funnel);
// {
//   visitors: 1000,
//   engaged: 300,
//   diagnosed: 150,
//   converted: 50
// }
```

### Анализ сегментов

```javascript
const segmentInsights = await api.getSegmentInsights('engaged');
// Детальная аналитика по сегменту
```

### Персонализированные рекомендации

```javascript
const recommendations = await api.getUserRecommendations(userId);
// {
//   segment: 'engaged',
//   next_best_actions: ['diagnostic_start', 'personal_path_view'],
//   content_suggestions: [...]
// }
```

## Производительность и масштабируемость

### Оптимизации

- **Индексы** на всех ключевых полях поиска
- **Партиционирование** по времени для больших таблиц
- **Кэширование** часто используемых данных сегментов
- **Асинхронная обработка** логирования

### Мониторинг

```sql
-- Активные сессии
SELECT COUNT(*) FROM site_sessions WHERE session_end IS NULL;

-- События за последний час
SELECT COUNT(*) FROM site_events
WHERE created_at >= datetime('now', '-1 hour');

-- Топ источников трафика
SELECT source, COUNT(*) as visits
FROM site_sessions
WHERE source IS NOT NULL
GROUP BY source
ORDER BY visits DESC;
```

## Безопасность и приватность

- **Анонимизация** чувствительных данных
- **GDPR compliance** - возможность удаления данных пользователя
- **Rate limiting** - защита от спама логами
- **Валидация данных** на всех эндпоинтах

## Расширение системы

### Добавление новых типов событий

1. Добавить метод в класс Database
2. Создать API эндпоинт
3. Обновить frontend SDK
4. Добавить в логику сегментации (при необходимости)

### Кастомные метрики

```python
# Добавление кастомной метрики
def calculate_custom_metric(user_id):
    # Логика расчета метрики
    return metric_value

# Интеграция в сегментацию
if custom_metric > threshold:
    user_segment['special_category'] = True
```

## Техническая поддержка

### Мониторинг системы

```bash
# Проверка работоспособности
curl http://localhost:5000/api/health

# Статистика базы данных
python test_database.py

# Обновление сегментов
python -m telegram-bot.user_segmentation
```

### Поиск проблем

```sql
-- События с ошибками
SELECT * FROM site_events
WHERE error_message IS NOT NULL
ORDER BY created_at DESC
LIMIT 10;

-- Пользователи без сегментов
SELECT u.user_id
FROM users u
LEFT JOIN site_sessions ss ON u.user_id = ss.tg_user_id
WHERE ss.id IS NULL;
```

## Заключение

Система обеспечивает:
- ✅ Полное отслеживание пользовательского пути
- ✅ Автоматическую сегментацию и персонализацию
- ✅ Масштабируемую архитектуру для роста
- ✅ Готовность к продвинутой аналитике
- ✅ Соответствие требованиям приватности

**Результат:** Единая платформа для глубокого понимания пользовательского поведения и оптимизации конверсии! 🎯