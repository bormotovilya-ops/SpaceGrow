# Настройка Groq на Vercel (БЕСПЛАТНО!)

## ✅ Да, Groq можно бесплатно использовать с Vercel!

**Groq** - отличный выбор, потому что:
- ✅ **Очень быстрый** - ответы за миллисекунды (быстрее чем OpenAI)
- ✅ **Бесплатный тариф** - до 14,400 запросов в день
- ✅ **Простая интеграция** - стандартный OpenAI-совместимый API
- ✅ **Качественные модели** - Llama 3.1, Mixtral и другие
- ✅ **Работает в России** - **VPN НЕ ТРЕБУЕТСЯ!** 🎉

## Пошаговая инструкция:

### Шаг 1: Получите API ключ Groq

1. Зарегистрируйтесь на https://console.groq.com
2. Войдите в аккаунт (можно через Google/GitHub)
3. Перейдите в **API Keys**
4. Нажмите **Create API Key**
5. Скопируйте ключ (начинается с `gsk_...`)
6. Сохраните его - понадобится на следующем шаге

### Шаг 2: Настройте переменные окружения в Vercel

1. Войдите в ваш проект на https://vercel.com
2. Перейдите в **Settings** → **Environment Variables**
3. Нажмите **Add New**
4. Заполните:
   - **Name:** `GROQ_API_KEY`
   - **Value:** ваш API ключ от Groq (начинается с `gsk_...`)
   - Выберите все окружения: ✅ Production, ✅ Preview, ✅ Development
5. Нажмите **Save**

### Шаг 3: Выберите файл API

У вас есть два варианта:

**Вариант A: Использовать Groq (рекомендуется)**
- Переименуйте `api/chat-groq.js` в `api/chat.js`
- Или используйте оба файла и выберите нужный

**Вариант B: Оставить GigaChat**
- Оставьте `api/chat.js` как есть (для GigaChat)
- Groq будет в `api/chat-groq.js`

### Шаг 4: Обновите фронтенд (если используете Groq)

Если используете `api/chat-groq.js`, обновите `src/components/ChatBot.jsx`:

```javascript
const response = await fetch('/api/chat-groq', {  // Измените на chat-groq
  // ... остальной код
})
```

Или просто переименуйте файл `api/chat-groq.js` в `api/chat.js` - тогда ничего менять не нужно!

### Шаг 5: Деплой проекта

```bash
git add .
git commit -m "Add Groq integration"
git push
```

Vercel автоматически задеплоит проект и Serverless Function будет работать!

## Стоимость:

- **Vercel:** Бесплатно (100GB-hours/месяц)
- **Groq:** Бесплатно (до 14,400 запросов/день)
- **Итого:** Полностью бесплатно! 🎉

## Лимиты Groq (бесплатный тариф):

- ✅ **14,400 запросов в день** - более чем достаточно для чат-бота
- ✅ **30 запросов в минуту** - хорошая скорость
- ✅ **Быстрые ответы** - обычно < 1 секунды
- ✅ **Качественные модели** - Llama 3.1, Mixtral

## Доступные модели Groq:

В файле `api/chat-groq.js` используется `llama-3.1-70b-versatile`, но можно выбрать:

- `llama-3.1-70b-versatile` - самая мощная (рекомендуется)
- `llama-3.1-8b-instant` - быстрая и легкая
- `mixtral-8x7b-32768` - хороший баланс скорости и качества
- `gemma-7b-it` - компактная модель

Чтобы изменить модель, отредактируйте строку в `api/chat-groq.js`:
```javascript
model: 'llama-3.1-70b-versatile', // Замените на нужную модель
```

## Преимущества Groq:

✅ **Скорость** - ответы в 10-30 раз быстрее чем OpenAI
✅ **Бесплатно** - щедрый бесплатный тариф
✅ **Простота** - стандартный OpenAI-совместимый API
✅ **Качество** - отличные open-source модели
✅ **Надежность** - стабильная работа

## Сравнение с GigaChat:

| Параметр | Groq | GigaChat |
|----------|------|----------|
| Скорость | ⚡ Очень быстрый | 🐢 Средняя |
| Бесплатный лимит | 14,400 запросов/день | 10,000 токенов/день |
| Работа в России | ✅ Да | ✅ Да |
| Качество ответов | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| Простота настройки | ✅ Очень просто | ⚠️ Может требовать OAuth |

**Рекомендация:** Groq - лучший выбор для начала!

## Важно: VPN не требуется!

✅ **Groq работает в России без VPN**
- API доступен напрямую
- Регистрация на console.groq.com работает без VPN
- Все запросы проходят без проблем
- Не нужно настраивать прокси или VPN

Если у вас проблемы с доступом:
- Проверьте интернет-соединение
- Попробуйте другой браузер
- В редких случаях может помочь VPN, но обычно не требуется

## Устранение неполадок:

### Ошибка "Groq API не настроен"
- Проверьте, что переменная `GROQ_API_KEY` добавлена в Vercel
- Убедитесь, что ключ начинается с `gsk_`
- Передеплойте проект после добавления переменной

### Ошибка 404 на `/api/chat-groq`
- Убедитесь, что файл `api/chat-groq.js` существует
- Или переименуйте его в `api/chat.js`
- Проверьте, что проект задеплоен на Vercel

### Ошибка "Rate limit exceeded"
- Вы превысили лимит 14,400 запросов/день
- Подождите до следующего дня или используйте другой API ключ

### Медленные ответы
- Groq обычно очень быстрый
- Если медленно, попробуйте модель `llama-3.1-8b-instant` (быстрее)
