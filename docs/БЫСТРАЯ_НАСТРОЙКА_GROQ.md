# Быстрая настройка Groq

## ⚠️ ВАЖНО: Токены нельзя добавлять в код!

Токены должны храниться только в:
- ✅ Переменных окружения Vercel (для продакшена)
- ✅ Локальном файле `.env` (для разработки, файл в .gitignore)

## Шаг 1: Локальная настройка (для тестирования)

1. Создайте файл `.env` в корне проекта (если его нет)
2. Добавьте туда ваш токен Groq:

```
GROQ_API_KEY=gsk_ваш_токен_здесь
```

3. Файл `.env` уже в `.gitignore`, так что он не попадет в Git

## Шаг 2: Настройка на Vercel (для продакшена)

1. Войдите в ваш проект на https://vercel.com
2. Перейдите в **Settings** → **Environment Variables**
3. Нажмите **Add New**
4. Заполните:
   - **Name:** `GROQ_API_KEY`
   - **Value:** ваш токен Groq (начинается с `gsk_...`)
   - Выберите все окружения: ✅ Production, ✅ Preview, ✅ Development
5. Нажмите **Save**
6. Передеплойте проект (Vercel сделает это автоматически при следующем push)

## Шаг 3: Проверка

После настройки:
- Локально: запустите `npm run dev` и проверьте работу чата
- На Vercel: после деплоя проверьте работу на вашем сайте

## Готово! 🎉

Теперь чат-бот будет использовать Groq API.

Файл `api/chat.js` уже настроен на Groq, просто добавьте токен в нужное место.
