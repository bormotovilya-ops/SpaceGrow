# 🚀 Быстрая инструкция по деплою

## Что нужно сделать:

### 1. Установить Git (если не установлен)
- Скачайте: https://git-scm.com/download/win
- Установите и перезапустите терминал

### 2. Инициализировать Git репозиторий

Откройте PowerShell в папке проекта и выполните:

```powershell
git init
git add .
git commit -m "Initial commit: Sales Funnel MiniApp"
```

### 3. Создать репозиторий на GitHub

1. Зайдите на https://github.com
2. Нажмите **"+"** → **"New repository"**
3. Название: `sales-funnel-miniapp` (или любое другое)
4. **НЕ** ставьте галочки на README, .gitignore, license
5. Нажмите **"Create repository"**

### 4. Подключить к GitHub

Скопируйте команды, которые покажет GitHub (они будут примерно такие):

```powershell
git remote add origin https://github.com/ВАШ_ЛОГИН/НАЗВАНИЕ_РЕПОЗИТОРИЯ.git
git branch -M main
git push -u origin main
```

**Если попросит авторизацию:**
- Создайте Personal Access Token: GitHub → Settings → Developer settings → Personal access tokens → Generate new token
- Выберите scope: `repo`
- Используйте токен вместо пароля

### 5. Деплой на Vercel

1. Зайдите на https://vercel.com
2. Нажмите **"Add New..."** → **"Project"**
3. Нажмите **"Import Git Repository"**
4. Выберите ваш репозиторий
5. Настройки уже указаны в `vercel.json`, просто нажмите **"Deploy"**

### (Опционально) Включить аналитику Яндекс.Метрики

Метрика уже подключена в коде. Чтобы включить её на Vercel, добавьте переменную окружения `VITE_YM_COUNTER_ID` (ID счётчика).

- Инструкция: см. [ENV_SETUP.md](./ENV_SETUP.md)

Готово! 🎉 Ваш сайт будет доступен по адресу `ваш-проект.vercel.app`

---

**Подробная инструкция:** см. файл [ДЕПЛОЙ.md](./ДЕПЛОЙ.md)


