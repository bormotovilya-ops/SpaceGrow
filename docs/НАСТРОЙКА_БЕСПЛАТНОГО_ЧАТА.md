# Настройка полностью бесплатного чата

## Два варианта:

### Вариант 1: Режим заглушки (работает сразу, без настройки)

**Преимущества:**
- ✅ Работает сразу, без регистрации
- ✅ Не требует API ключей
- ✅ Полностью бесплатно
- ✅ Быстрые ответы

**Как использовать:**
1. Добавьте в `.env`:
   ```env
   USE_MOCK_RESPONSES=true
   ```
2. Перезапустите сервер
3. Готово! Чат будет отвечать предопределенными ответами

**Как расширить ответы:**
- Откройте `server-local.js`
- Найдите функцию `handleMockResponse`
- Добавьте свои вопросы и ответы в объект `responses`

### Вариант 2: Hugging Face Inference API (бесплатный)

**Преимущества:**
- ✅ Полностью бесплатно (с лимитами)
- ✅ Не требует кредитной карты
- ✅ Более гибкие ответы
- ✅ Много моделей на выбор

**Как использовать:**
1. Зарегистрируйтесь на https://huggingface.co
2. Создайте API токен: Settings → Access Tokens → New token
3. Добавьте в `.env`:
   ```env
   HF_API_KEY=ваш_токен_от_huggingface
   ```
4. Убедитесь, что `USE_MOCK_RESPONSES` не установлен в `true`
5. Перезапустите сервер

**Лимиты Hugging Face:**
- Бесплатный тариф: ограниченное количество запросов
- Модели могут загружаться при первом запросе (занимает время)

## Рекомендация:

**Начните с режима заглушки** - он работает сразу и бесплатно. Потом, если нужно, переключитесь на Hugging Face API.

## Для деплоя на Vercel:

### Режим заглушки:
- Добавьте в Vercel Environment Variables:
  - `USE_MOCK_RESPONSES=true`

### Hugging Face:
- Добавьте в Vercel Environment Variables:
  - `HF_API_KEY=ваш_токен`

## Готово!

После настройки чат будет работать полностью бесплатно! 🎉
