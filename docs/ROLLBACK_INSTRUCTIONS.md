# Инструкция по откату изменений

## 🛡️ Безопасный откат на Vercel

Если после публикации что-то сломалось, можно быстро откатить изменения.

### Вариант 1: Откат через Git (рекомендуется)

1. **Откатить изменения в локальном репозитории:**
   ```bash
   # Откатиться к последнему рабочему коммиту
   git reset --hard HEAD
   
   # Или к конкретному коммиту (например, к тегу backup)
   git reset --hard backup-before-reorganization
   ```

2. **Отправить изменения на GitHub:**
   ```bash
   git push origin main --force
   ```

3. **Vercel автоматически пересоберет проект** с откатанной версией

### Вариант 2: Откат через Vercel Dashboard

1. Откройте https://vercel.com
2. Выберите ваш проект
3. Перейдите в раздел **Deployments**
4. Найдите последний рабочий deployment (до ваших изменений)
5. Нажмите на три точки (⋮) рядом с deployment
6. Выберите **Promote to Production**

### Вариант 3: Откат конкретных файлов

Если нужно откатить только определенные файлы:

```bash
# Откатить только api/chat.js к версии из HEAD
git checkout HEAD -- api/chat.js

# Откатить только scripts/server-local.js (если он был в корне)
git checkout HEAD -- server-local.js

# Отправить изменения
git add api/chat.js
git commit -m "Rollback: восстановлен рабочий api/chat.js"
git push origin main
```

## 📋 Текущие теги и коммиты

- **backup-before-reorganization** - тег создан перед публикацией реорганизованной версии
- **HEAD (5dcb10c)** - последний рабочий коммит: "Update welcome message in Telegram bot"

## ⚠️ Важно

- Всегда делайте коммит перед публикацией
- Создавайте теги для важных версий
- Проверяйте работу локально перед push в main

## 🔄 Быстрый откат (если что-то сломалось)

```bash
# 1. Откатить к последнему рабочему коммиту
git reset --hard HEAD

# 2. Принудительно отправить на GitHub
git push origin main --force

# 3. Vercel автоматически пересоберет проект
```

**Время восстановления: ~2-3 минуты**
