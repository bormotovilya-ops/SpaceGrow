# Просмотр базы данных и логов бота

## 📊 Просмотр базы данных

### Способ 1: Использовать скрипт `view_db.py`

Запустите скрипт для просмотра содержимого базы данных:

```bash
cd telegram-bot
python view_db.py
```

Скрипт покажет:
- Всех пользователей
- Их статусы (начали ли диагностику)
- Отправлены ли напоминания
- Временные метки
- Статистику

### Способ 2: Использовать SQLite напрямую

Файл базы данных находится в папке `telegram-bot` и называется `bot_users.db`

#### Windows:
1. Установите SQLite: https://www.sqlite.org/download.html
2. Откройте командную строку в папке `telegram-bot`
3. Выполните: `sqlite3 bot_users.db`
4. Команды для просмотра:
   ```sql
   .tables              -- Показать все таблицы
   SELECT * FROM users; -- Показать всех пользователей
   .headers on          -- Включить заголовки
   .mode column         -- Табличный формат
   ```

#### Онлайн инструменты:
- https://sqliteviewer.app/ - загрузите файл `bot_users.db`
- https://inloop.github.io/sqlite-viewer/ - еще один онлайн просмотрщик

### Способ 3: Python скрипт (интерактивный)

```python
import sqlite3

conn = sqlite3.connect('telegram-bot/bot_users.db')
cursor = conn.cursor()

cursor.execute('SELECT * FROM users')
for row in cursor.fetchall():
    print(row)

conn.close()
```

## 📝 Просмотр логов

### Способ 1: Логи в консоли

Когда вы запускаете бота:
```bash
cd telegram-bot
python bot.py
```

Все логи выводятся в консоль. Вы увидите:
- `Планировщик задач запущен` - планировщик работает
- `Пользователь X создан/обновлен` - пользователь использовал /start
- `Проверка напоминаний запущена` - каждую минуту
- `Найдено пользователей для первого напоминания: X` - количество

### Способ 2: Сохранить логи в файл

Измените `bot.py`, добавьте сохранение логов:

```python
import logging

logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s',
    level=logging.INFO,
    handlers=[
        logging.FileHandler('bot.log'),
        logging.StreamHandler()  # Также выводить в консоль
    ]
)
```

Тогда логи будут в файле `telegram-bot/bot.log`

### Способ 3: При запуске через PowerShell (Windows)

Перенаправьте вывод в файл:
```powershell
cd telegram-bot
python bot.py > bot_output.log 2>&1
```

Логи будут в файле `bot_output.log`

## 🔍 Что проверять в логах

1. **При запуске бота:**
   - ✅ `Планировщик задач запущен` - должно быть
   - ✅ `JobQueue не доступен!` - НЕ должно быть

2. **Когда пользователь нажимает /start:**
   - ✅ `Пользователь X создан/обновлен в БД`
   - ✅ `started_at установлен на CURRENT_TIMESTAMP`

3. **Каждую минуту:**
   - ✅ `Проверка напоминаний запущена`
   - ✅ `Запрос первого напоминания выполнен`
   - ✅ `Найдено пользователей для первого напоминания: X`

4. **При отправке напоминания:**
   - ✅ `Отправка первого напоминания пользователю X`
   - ✅ `Первое напоминание отправлено пользователю X`

## 📍 Где находятся файлы

- **База данных:** `telegram-bot/bot_users.db` (создается автоматически)
- **Скрипт просмотра:** `telegram-bot/view_db.py`
- **Логи:** В консоли при запуске, или в файле (если настроено)

## ⚠️ Важно

- База данных создается автоматически при первом запуске бота
- Если файла `bot_users.db` нет, значит бот еще не запускался
- Логи в консоли видно только пока бот работает
- Для постоянного мониторинга лучше сохранять логи в файл
