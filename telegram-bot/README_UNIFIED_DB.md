# Единая база данных пользователей

## Обзор

Теперь у вас есть единая база данных, которая объединяет данные пользователей из Telegram бота, MiniApp и сайта. Это позволяет отслеживать весь путь пользователя и строить поведенческую аналитику.

## Архитектура

### Основные таблицы

1. **`users`** - существующие пользователи Telegram бота
2. **`user_identities`** - связи между различными идентификаторами
3. **`site_sessions`** - сессии сайта и MiniApp
4. **`site_events`** - события пользователя для аналитики
5. **`diagnostics_results`** - результаты диагностики

### Связи между таблицами

```
users (tg_user_id) ──┬── user_identities (tg_user_id)
                     │
                     ├── site_sessions (tg_user_id)
                     │
                     ├── site_events (tg_user_id)
                     │
                     └── diagnostics_results (tg_user_id)

site_sessions (id) ─── site_events (session_id)
```

## Быстрый старт

### 1. Запуск миграций

```bash
python run_migrations.py
```

### 2. Проверка работы

```bash
python test_database.py
```

### 3. Запуск backend

```bash
cd backend
python app.py
```

## Основные сценарии использования

### Сценарий 1: Пользователь заходит на сайт

1. **Создание сессии:**
   ```javascript
   const sessionId = await fetch('/api/track-session', {
     method: 'POST',
     body: JSON.stringify({ cookie_id: cookieId, action: 'start' })
   });
   ```

2. **Логирование событий:**
   ```javascript
   await fetch('/api/track-event', {
     method: 'POST',
     body: JSON.stringify({
       session_id: sessionId,
       event_type: 'page_view',
       event_name: 'home_page_loaded'
     })
   });
   ```

### Сценарий 2: Пользователь открывает MiniApp из Telegram

1. **Связывание идентификаторов:**
   ```javascript
   await fetch('/api/link-identities', {
     method: 'POST',
     body: JSON.stringify({ tg_user_id: tgUserId, cookie_id: cookieId })
   });
   ```

2. **Все последующие события** теперь связаны с Telegram пользователем

### Сценарий 3: Диагностика

1. **Сохранение результатов:**
   ```javascript
   await fetch('/api/diagnostics', {
     method: 'POST',
     body: JSON.stringify({
       tg_user_id: tgUserId,
       result_data: diagnosticResults
     })
   });
   ```

## Аналитические возможности

### Общая статистика сайта

```bash
curl http://localhost:5000/api/analytics/site
```

### Аналитика пользователя

```bash
curl http://localhost:5000/api/user/123/analytics
```

### События пользователя

```bash
curl http://localhost:5000/api/user/123/events?limit=50
```

## Типичные запросы к базе данных

### Найти пользователя по cookie

```python
user = db.get_user_by_cookie('abc123def456')
```

### Получить активные сессии

```python
sessions = db.get_active_sessions('abc123def456')
```

### Связать Telegram с сайтом

```python
success = db.link_telegram_to_cookie(123456789, 'cookie123', 'miniapp')
```

### Логировать событие

```python
db.log_event(
    session_id=1,
    event_type='click',
    event_name='start_diagnostic',
    page='/diagnostics',
    metadata={'button_id': 'start'}
)
```

## Мониторинг и поддержка

### Проверка здоровья

```bash
curl http://localhost:5000/api/health
```

### Логи

Все действия логируются. Основные логи:
- Создание/обновление пользователей
- Связывание идентификаторов
- Ошибки операций с БД

### Производительность

- Все тяжелые запросы проиндексированы
- Используется SQLite с оптимизациями
- Рекомендуется регулярная очистка старых сессий

## Расширение системы

### Добавление новых типов событий

```python
# В db.log_event() добавить новый event_type
db.log_event(session_id, 'new_event_type', 'event_name', ...)
```

### Добавление новых таблиц

1. Создать миграцию в `migrations.py`
2. Добавить методы в класс `Database`
3. Создать API эндпоинты в `backend/app.py`

## Troubleshooting

### База данных не инициализируется

```bash
# Проверить наличие файла
ls -la telegram-bot/bot_users.db

# Перезапустить миграции
python run_migrations.py
```

### Ошибки в API

```bash
# Проверить логи backend
tail -f backend/logs/app.log

# Проверить доступность
curl http://localhost:5000/api/health
```

### Проблемы с производительностью

```bash
# Проверить размер БД
ls -lh telegram-bot/bot_users.db

# Оптимизировать индексы
sqlite3 telegram-bot/bot_users.db "REINDEX;"
```

## Следующие шаги

1. **Интеграция с frontend** - добавить трекинг в React компоненты
2. **A/B тестирование** - отслеживать эффективность разных версий
3. **Экспорт данных** - для внешних аналитических систем
4. **Резервное копирование** - автоматизировать бэкапы БД
5. **Масштабирование** - при росте нагрузки перейти на PostgreSQL/MySQL

---

🎯 **Результат:** Единая система отслеживания пользователей от первого контакта в Telegram до завершения диагностики на сайте!