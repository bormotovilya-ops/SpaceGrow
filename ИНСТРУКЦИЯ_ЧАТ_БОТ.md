# Инструкция по настройке чат-бота с нейросетью

## Описание

Добавлен чат-бот с интеграцией OpenAI, который отвечает на вопросы клиентов от имени Ильи Бормотова. Чат-бот использует информацию о бизнесе, услугах и подходе для ответов на вопросы.

## Настройка

### 1. Установка зависимостей backend

```bash
cd backend
pip install -r requirements.txt
```

### 2. Настройка OpenAI API Key

1. Создайте файл `.env` в папке `backend/` (можно скопировать из `backend/.env.example`)
2. Получите API ключ на https://platform.openai.com/api-keys
3. Добавьте ключ в файл `.env`:

```
OPENAI_API_KEY=sk-your-api-key-here
```

### 3. Запуск

**Backend:**
```bash
cd backend
python app.py
```

**Frontend:**
```bash
npm run dev
```

## Использование

1. Кнопка чата временно скрыта (можно вернуть в Header.jsx)
2. При клике открывается модальное окно чата
3. Пользователь может задавать вопросы о услугах, опыте, подходе и т.д.
4. Нейросеть отвечает от имени Ильи Бормотова, используя контекст сайта

## Деплой на Vercel (БЕСПЛАТНО!)

### Вариант 1: Vercel Serverless Functions (рекомендуется)

✅ **Полностью бесплатно!**
- Vercel предоставляет 100GB-hours бесплатно в месяц
- Для чат-бота этого более чем достаточно
- Не нужен отдельный сервер

**Что нужно:**
1. Файл `api/chat.js` уже создан
2. Добавьте `GIGACHAT_API_KEY` в Environment Variables в Vercel
3. Деплойте проект - всё работает!

### Вариант 2: Python Backend на отдельном хостинге

Если нужен Python Flask:
- **Render.com** - бесплатный тариф (с ограничениями)
- **Railway.app** - $5 кредитов бесплатно
- **Fly.io** - бесплатный тариф

⚠️ **Важно:** На Vercel Python Flask не работает напрямую, нужны Serverless Functions (Node.js) или отдельный хостинг.

## Технические детали

### Backend (`backend/app.py`)
- Endpoint: `/api/chat` (POST)
- Использует модель: `gpt-4o-mini` (более дешевая версия)
- Контекст включает информацию о:
  - Опыте и специализации
  - Услугах и подходе
  - Технологическом стеке
  - Контактах

### Frontend (`src/components/ChatBot.jsx`)
- Компонент модального окна с чатом
- Адаптивный дизайн для мобильных устройств
- Анимации и индикатор загрузки

## Бесплатные альтернативы OpenAI

### 1. **GigaChat (Сбер)** - Рекомендуется для России
- ✅ Бесплатный API с лимитами
- ✅ Отличная поддержка русского языка
- ✅ Работает в России без VPN
- 📝 Регистрация: https://developers.sber.ru/gigachat
- 💰 Бесплатный тариф: до 10,000 токенов/день

### 2. **Ollama (Локально)** - Полностью бесплатно
- ✅ Работает на вашем сервере
- ✅ Нет лимитов и ограничений
- ✅ Приватность данных
- 📝 Установка: https://ollama.ai
- 💰 Полностью бесплатно

### 3. **Groq** - Быстрый и бесплатный ⭐ РЕКОМЕНДУЕТСЯ!
- ✅ Очень быстрые ответы (в 10-30 раз быстрее OpenAI)
- ✅ Бесплатный тариф: до 14,400 запросов/день
- ✅ Отличное качество ответов
- ✅ Работает с Vercel бесплатно
- ✅ OpenAI-совместимый API (очень просто настроить)
- ✅ **VPN НЕ ТРЕБУЕТСЯ** - работает в России напрямую
- 📝 API: https://console.groq.com
- 💰 Бесплатно: до 14,400 запросов/день, 30 запросов/минуту
- 📄 Инструкция: см. `GROQ_VERCEL_SETUP.md`

### 4. **Together AI** - Бесплатный тариф
- ✅ Множество моделей на выбор
- ✅ Бесплатный tier доступен
- 📝 API: https://together.ai
- 💰 Бесплатно: $25 кредитов при регистрации

### 5. **Hugging Face Inference API**
- ✅ Бесплатно для некоторых моделей
- ✅ Много open-source моделей
- 📝 API: https://huggingface.co/inference-api
- 💰 Бесплатно: ограниченное количество запросов

## Стоимость OpenAI (если используете)

Используется модель `gpt-4o-mini`, которая значительно дешевле GPT-4:
- ~$0.15 за 1M входных токенов
- ~$0.60 за 1M выходных токенов

Для типичного вопроса с ответом это примерно $0.001-0.002 за запрос.

## Безопасность

⚠️ **Важно:** Не коммитьте файл `.env` с API ключом в Git!

Убедитесь, что `.env` добавлен в `.gitignore`:
```
backend/.env
```

## Устранение неполадок

### Ошибка "OpenAI API не настроен"
- Проверьте, что файл `.env` существует в папке `backend/`
- Убедитесь, что `OPENAI_API_KEY` указан правильно
- Перезапустите backend после изменения `.env`

### Ошибка подключения к серверу
- Убедитесь, что backend запущен на порту 5000
- Проверьте настройки proxy в `vite.config.js`

### Высокая стоимость API
- Можно переключиться на более дешевую модель в `backend/app.py`:
  ```python
  model="gpt-3.5-turbo"  # Вместо gpt-4o-mini
  ```
- Или использовать бесплатные альтернативы (см. раздел выше)

## Переключение на GigaChat + Vercel (БЕСПЛАТНО!)

### ✅ Да, нужен backend, но можно использовать Vercel Serverless Functions (бесплатно!)

**Преимущества:**
- ✅ Полностью бесплатно на Vercel (бесплатный тариф включает 100GB-hours в месяц)
- ✅ Не нужен отдельный сервер
- ✅ API ключ хранится безопасно в переменных окружения Vercel
- ✅ Автоматическое масштабирование
- ✅ Работает в России без VPN

### Настройка GigaChat на Vercel:

1. **Зарегистрируйтесь на GigaChat:**
   - Перейдите на https://developers.sber.ru/gigachat
   - Зарегистрируйтесь и получите API ключ

2. **Создайте Serverless Function:**
   - Файл `api/chat.js` уже создан в проекте
   - Это Vercel Serverless Function, которая работает бесплатно

3. **Настройте переменные окружения в Vercel:**
   - Войдите в ваш проект на Vercel
   - Перейдите в Settings → Environment Variables
   - Добавьте переменную:
     - **Name:** `GIGACHAT_API_KEY`
     - **Value:** ваш API ключ от GigaChat
   - Выберите все окружения (Production, Preview, Development)
   - Нажмите Save

4. **Обновите фронтенд:**
   - В `src/components/ChatBot.jsx` уже используется `/api/chat`
   - Vercel автоматически направит запросы к Serverless Function

5. **Деплой:**
   ```bash
   git add .
   git commit -m "Add GigaChat integration"
   git push
   ```
   - Vercel автоматически задеплоит проект

### Альтернатива: Python Backend на отдельном хостинге

Если хотите использовать Python Flask backend:

1. **Бесплатные варианты для Python:**
   - **Render.com** - бесплатный тариф (с ограничениями)
   - **Railway.app** - $5 кредитов бесплатно при регистрации
   - **Fly.io** - бесплатный тариф с ограничениями

2. **Обновите `backend/app.py` для GigaChat:**
   ```python
   from giga import GigaChat
   
   giga_client = GigaChat(credentials=os.getenv('GIGACHAT_API_KEY'))
   
   # В endpoint /api/chat:
   response = giga_client.chat(messages)
   ```

### Ollama (локально, полностью бесплатно)

1. Установите Ollama: https://ollama.ai
2. Скачайте модель:
   ```bash
   ollama pull llama2
   ```
3. Обновите `backend/app.py` для работы с локальным API Ollama

### Groq (быстрый и бесплатный)

1. Зарегистрируйтесь на https://console.groq.com
2. Получите API ключ
3. Установите библиотеку:
   ```bash
   pip install groq
   ```
4. Обновите код для использования Groq API
